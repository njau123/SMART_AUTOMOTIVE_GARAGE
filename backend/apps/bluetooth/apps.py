from django.apps import AppConfig


class BluetoothConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.bluetooth"
    verbose_name = "Bluetooth Device Management"